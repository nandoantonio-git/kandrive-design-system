import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as TagOrgModeStories from './TagOrgMode.stories';

<Meta of={TagOrgModeStories} />

# TagOrgMode (Figma: `atom/TagOrgMode`, `1421:18769`)

## Uso

Tag que identifica o modo de organização ativo (Data/Livre/Projeto/Tipo) — usada nos fluxos de criação/edição de template de organização, para deixar claro qual critério está sendo aplicado. Aparece em `organism/OrganizeFreeModeCanvas`.

<Canvas of={TagOrgModeStories.Free} />
<Controls />

<Canvas of={TagOrgModeStories.AllModes} />

## Modos

| Valor da prop `mode` | Rótulo exibido |
| --- | --- |
| `free` (padrão) | Modo Livre |
| `date` | Data |
| `project` | Projeto |
| `type` | Tipo |

## Estados

Elemento de exibição estática — sem hover/pressed/disabled próprios, é um rótulo do sandbox de organização.
