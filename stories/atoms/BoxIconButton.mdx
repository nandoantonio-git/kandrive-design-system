import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as BoxIconButtonStories from './BoxIconButton.stories';

<Meta of={BoxIconButtonStories} />

# BoxIconButton (Figma: `atom/boxIconButton`, `1431:20102`)

## Uso

Botão-ícone quadrado (40px) do toolbar de ações do canvas de **Organização em Modo Livre** — adicionar nodo, expandir, redefinir, excluir. Use só dentro desse toolbar; pra ações genéricas de UI prefira `PushButton` (Regra 1) ou os ícone-botões sem chrome (`ClearButton`/`ConfirmButton`/etc.).

<Canvas of={BoxIconButtonStories.Default} />
<Controls />

## Variantes

4 glifos no toolbar: adicionar nodo, expandir, redefinir e excluir. O glifo de excluir usa a cor de perigo (`destructive/35`) em hover/clicked; os outros 3 usam a paleta secundária padrão.

<Canvas of={BoxIconButtonStories.Toolbar} />
<Canvas of={BoxIconButtonStories.Danger} />
<Canvas of={BoxIconButtonStories.Disabled} />

## Estados

`Idle` · `Hover` · `Clicked` (com `scale-95` no press) · `Disabled`. Sem loading/error — é um botão de toolbar, sem operação assíncrona própria.

## Terminologia

Não renderiza texto visível — só `aria-label` por instância ("Adicionar nodo"/"Expandir"/"Redefinir"/"Excluir").
