import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as DeleteButtonStories from './DeleteButton.stories';

<Meta of={DeleteButtonStories} />

# DeleteButton (`atom/DeleteButton`)

## Uso

Ícone-botão sem chrome de borda pra **ações destrutivas** pontuais (remover um item de uma lista, por exemplo) — diferente do `PushButton` com `isDestructive`, que é pra ação primária de destruição de uma tela inteira (ex. "Excluir" num modal).

<Canvas of={DeleteButtonStories.Default} />
<Controls />

## Variantes de cor

`style`: `Default` · `Red` · `White`.

<Canvas of={DeleteButtonStories.Red} />
<Canvas of={DeleteButtonStories.White} />
<Canvas of={DeleteButtonStories.Disabled} />

## Estados

`Idle` · `Clicked` (feedback via `scale-90`) · `Disabled`. Sem loading/error.

## Terminologia

Não renderiza texto visível — `aria-label="Excluir"`.
