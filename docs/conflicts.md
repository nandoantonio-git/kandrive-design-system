# Conflicts

Log de conflitos entre o que foi encontrado no Figma e as decisões de design
já travadas em `AGENTS.md`. Nenhum conflito é resolvido silenciosamente aqui
— apenas registrado, com urgência e justificativa, para decisão humana.

Tabela consolidada e ordenada por urgência decrescente (alta > média >
baixa); a única entrada de infraestrutura já resolvida fica ao final, fora
da ordenação de urgência.

| Componente | Conflito | Urgência | Justificativa da urgência |
| --- | --- | --- | --- |
| `celule/chip/folder-tag` (`atom molecules/FolderTagChip`, `1421:19040`, estado `Default`/não-expandido) | 🆕 **Achado novo em 2026-08-11 (US-026, auditoria de ponto-fixo).** `get_design_context` no estado `Default` (`isExpanded=false`) mostra o chip **sem** o ícone de pasta nem o rótulo visíveis — só um ícone utilitário à direita, sem nome semântico confirmado — bem diferente da implementação atual, que sempre mostra ícone+rótulo (variando só o truncamento) em todos os estados. Não alterado: o significado exato do estado "colapsado" não é claramente confirmável a partir do Figma sozinho (Regra 9) — mudar por inferência arriscaria uma UX pior (chip sem conteúdo visível por padrão). Ver `stories/molecules/FolderTagChip.mdx`. | média | Ambiguidade real de composição no próprio Figma (não uma escolha de implementação errada) — o chip continua funcional e visualmente coerente hoje; mudar por adivinhação seria pior que manter o gap documentado. Recomenda-se decisão humana sobre o que o estado colapsado deveria realmente mostrar. |
| `molecule/Label` (`1421:18687`) vs. `molecule/DropdownSelect/Label` (`1439:19650`) | 🆕 **Achado novo em 2026-08-11 (US-024).** `get_design_context` nos dois nodes confirma que são estruturalmente quase idênticos — mesma legenda "ETIQUETAR", mesma pílula `atom/Icon/Label` + "Etiquetar" + chevron, mesmo `atom/DropdownSelect/Label/Item` ("+ Nova Etiqueta") no rodapé da lista. Implementados como **2 componentes de código separados** (`label.tsx`/`dropdown-select-label.tsx`) por serem 2 node IDs Figma distintos — nenhuma consolidação feita silenciosamente. Ver detalhamento comparativo em `stories/molecules/Label.mdx`. | média | Provável duplicação não intencional no arquivo Figma fonte (2 componentes cobrindo o mesmo conceito de UI). Não bloqueia nenhum fluxo — ambos renderizam corretamente — mas gera 2 fontes de verdade para manter em sincronia. Recomenda-se decisão humana sobre (a) qual node é o canônico e deve ser descontinuado no Figma, e (b) se os componentes de código devem ser consolidados numa API única. |
| `atom/badge/TypeLabel` (pontos `kind="video"` e `kind="other"`) | 🆕 **Achado novo em 2026-08-11 (US-024), estendido em 2026-08-12 (US-026, 2ª auditoria de ponto-fixo).** `get_design_context` no nó `1421:18687` (`molecule/Label`) confirma o hex do ponto de tipo "Videos": `#b5254a` (`brand-pink-dark`). A releitura desta US confirma que `kind="other"` também reusa o mesmo par de marca, na variante clara: `#e8476a` (`brand-pink-light`) — o código tinha esse ponto como `bg-zinc-400` com um comentário dizendo "sem instância Figma observada", o que não é mais verdade; corrigido para `bg-brand-pink-light` nesta US. Ambos os pontos (`video`=dark, `other`=light) reusam o par de cor que a Regra 3 reserva exclusivamente à categoria semântica "Acesso rápido". Implementado literal (Regra 9), propagado em `type-label.tsx`. | baixa | Dois pontos decorativos de 7px num dropdown de etiquetas, não um indicador de tier de storage — risco de confusão semântica real com "Acesso rápido" é baixo neste contexto, mas é uma reutilização de token fora do significado que a Regra 3 declarou. Recomenda-se confirmar no Figma se a intenção é reusar a mesma cor de marca ou se os pontos de "Videos"/"Outros" deveriam ter cor própria. |
| `molecule/context-header` (rótulo "X itens selecionado") | 🆕 **Achado novo em 2026-08-11 (US-023).** `get_design_context` no nó `1421:19589` confirma `font-['Manrope:Medium']` no rótulo do contador — a Regra 4 trava Figtree como tipografia única do design system inteiro (mesmo gap já corrigido globalmente em US-011 para o restante da página). Implementado com Figtree (`font-sans`), não Manrope, sem resolver silenciosamente. | baixa | Um único rótulo de texto num toolbar de seleção contextual — não é um fluxo ao vivo crítico como "Guardar" (Regra 1), e a Regra 4 já trata Figtree como decisão travada geral; risco de exposição baixo. Recomenda-se o Figma corrigir a fonte desse nó na próxima revisão. |
| `organism/Card/Login` | ✅ **REDUZIDO em 2026-08-10 (usuário ajustou o componente no Figma)**: releitura confirma que a maior parte do nó agora usa `Figtree` (headings, labels, link "Esqueceu sua senha?", divisor "OU", botões sociais), e o botão "Entrar"/link já usam `#007e96` (token de marca correto, Regra 3). Residual baixo: 2 nós ainda especificam `Manrope` no Figma (placeholder dos inputs, label do botão "Entrar") — mantido `Figtree` no código mesmo assim (Regra 4, decisão travada), registrado como divergência menor, não Figma-confirmada como fato. Paleta neutra (`#1a1c1c`/`#3e484c`/`#bec8cc`/`#f9f9f9`) segue aproximada pela rampa Zinc — mesmo gap já coberto pela linha "Paleta neutra" abaixo, não é um item novo. Ver `stories/organisms/CardLogin.mdx`. | baixa | Rebaixado de alta: a divergência real hoje é só 2 nós de texto (não o componente inteiro), e o acento de cor principal já bate com a marca — exposição muito menor que o achado original. |
| `molecule/StorageBar` (variante de barra única, `tier="quick-access"`) | ✅ **RESOLVIDO em 2026-08-10 (usuário ajustou o componente no Figma)**: `storage-bar.tsx` corrigido para usar rosa (`brand-pink-dark`) em vez de teal para `tier="quick-access"` — consistente com `StorageBarExpanded` e com o token real do Figma. `organism/storage-sidebar` consome via prop `tier`, herda a correção automaticamente sem precisar de edição própria. | resolvida | — |
| `organism/FAQ/info/cards/colapsed` (tópico `FrequentIssues`) | 🆕 **Achado novo em 2026-08-10 (US-013).** A resposta Figma-confirmada de '"Liberar espaço" está desabilitado, isso é um erro?' usa a palavra "elegíveis" em prosa corrida ("arquivos duplicados ou **elegíveis** para guardar no longo prazo"). A Regra 5 proíbe "elegível" como texto visível, mas foi escrita pensando em **rótulo de UI** (badge/tag/chip), não em palavra dentro de uma resposta de FAQ em português corrente, onde "elegível" é vocabulário comum, não jargão técnico do produto. Mantido literal (Figma-confirmado, Regra 9) — ver `stories/organisms/FaqInfoCardCollapsed.mdx`. | baixa | Não bloqueia nenhum fluxo ao vivo (é texto de ajuda/FAQ, não um rótulo persistente de UI) — decisão humana bem-vinda sobre se a Regra 5 deve se estender a texto corrido/prosa ou permanecer restrita a rótulos de componente. |
| `cor/marca/primária` e `cor/marca/secundária` | ✅ **RESOLVIDO em 2026-08-10 (decisão humana)**: Figma é a fonte de verdade. Regra 3 atualizada para os valores reais (`#007e96` primária, `#31302d` secundária) — `#2A7A8C`/`#3A3C38` descontinuados. `--brand-teal`/`--brand-secondary` em `src/index.css` e `stories/tokens/Colors.mdx` propagados nesta US (US-009) para os novos valores. | resolvida | — |
| Nomenclatura de variáveis de cor | As variáveis Figma reais seguem o formato `var(--categoria-papel-valor)` (kebab-case), não `cor/categoria/papel/valor-semântico` como a Regra 2 prescreve. Duas variáveis de marca (`Brand/Theme/Pink/Dark`, `Brand/Theme/Pink/Light`) usam um terceiro formato (`Categoria/Grupo/Papel`). Achado #9 do inventário US-002, confirmado nesta US. | alta | A Regra 2 é uma decisão de nomenclatura de design system, não de valor — sem correspondência exata entre o formato prescrito e o real, toda tradução feita em `stories/tokens/Colors.mdx` (coluna "Token semântico") é uma tradução própria do design system, não o nome literal do Figma. Precisa de decisão humana sobre se a Regra 2 deve virar uma convenção de *tradução* (Figma usa um formato, código usa outro) ou se o Figma deve ser renomeado. |
| `molecule/StorageBar` (`Style=Expanded`) | ✅ **RESOLVIDO em 2026-08-10 (decisão humana)**: rosa (`Brand/Theme/Pink/Dark`/`Light`) deixa de ser "só branding" — vira a cor semântica oficial de **"Acesso rápido"**, usada propositalmente em `StorageBar` e qualquer componente dessa categoria. Regra 3 atualizada. | resolvida | — |
| Tipografia abaixo do piso de 16px | ✅ **RESOLVIDO em 2026-08-10 (US-011), no nível de token/átomo-base.** Regra 4 (política de exceção definida no mesmo dia) aplicada: `Type/Button/MD` corrigido de 14px para 16px na implementação real de `atom/PushButton` (`text-base`), deixa de ser exceção. `Type/Tag` corrigido de 8px (abaixo do piso de exceção) para 11px — nenhum componente implementava o valor antigo literalmente. `Type/Caption/SM` (11px), `Type/Body/XS` (10px), `Type/Body/XS/Bold` (10px) e `Type/Body/SM` (13px) permanecem exceção documentada (microtexto complementar); os 6 usos de `text-[10px]` (px fixo) encontrados no código, em 5 arquivos (`DropdownSelectGroupBy`, `ViewModeToggle`, `UploadPopover`, `DialogSaveOrganizationModal`, `OrganizeFreeModeCanvas`), foram corrigidos para `text-[0.625rem]` (rem). Ver `stories/tokens/Typography.mdx`. | resolvida | — |
| `atom/PushButton` — instâncias com label abaixo do piso via `className` | ⏸️ **DECISÃO HUMANA em 2026-08-10: manter como está.** Embora o átomo `PushButton` já use `text-base` (16px) por padrão, 8 organisms (`StorageSidebar`, `ArchiveBrowserModal`, `CleanSpaceStorage`, `OrganizePanelDropZone`, `OrganizeFreeModeCanvas`, `SaveOrganizationModal`, `TemplateReviewModal`, `SaveLongTermFileStorage`) sobrescrevem o label via `className="... text-xs"` em contextos de botão compacto (`h-8`), reintroduzindo texto de ação a 12px — abaixo do piso obrigatório da Regra 4 nesses rótulos reais. Usuário optou explicitamente por não alterar (cada layout compacto foi reconciliado contra seu próprio nó Figma; subir a fonte quebraria a largura/altura pretendida). | baixa | Rebaixado de alta por decisão humana explícita — gap de acessibilidade conhecido e aceito, não pendente de ação. Reabrir só se o usuário pedir. |
| `organism/Sidebar`, `organism/storage-sidebar`, `organism/cleanSpaceStorage` | ✅ **RESOLVIDO em 2026-08-10 (decisão humana), implementado em US-010**: das 3 variantes concorrentes ("Liberar Espaço"/"Gerir Espaço"/"Limpar Espaço"), a decisão fixa cada uma a um contexto — Sidebar (painel embutido/persistente) = **"Gerir Espaço"** (`Sidebar.tsx` sobrescreve via prop `manageSpaceLabel`); página de Armazenamento e Configurações de Plano = **"Liberar Espaço"** (default de `StorageSidebar.tsx`, era proibido, agora aprovado nesse contexto), dando acesso ao componente do nó `1439:16908`. **"Limpar Espaço"** (título antigo desse nó) fica proibido — `CleanSpaceStorage.tsx` retitulado para "Liberar Espaço" (título Figma-confirmado atual). Regra 5 atualizada. | resolvida | — |
| Paleta neutra (`neutral-*`, `ui-*`) | Só 4 dos ~30 papéis neutros Figma-confirmados batem exatamente com um degrau da rampa Zinc do Tailwind (`neutral-text-primary`=`zinc-950`, `neutral-text-secondary`=`zinc-700`, `neutral-text-tertiary`=`zinc-500`, `neutral-surface-medium`=`zinc-600`). A maioria — incluindo toda a família `ui-*` (`ui-input-border` `#3f4850`, `ui-connector-border`, `ui-button-border-ghost`) e vários papéis de borda/superfície/placeholder (`neutral-border-light` `#bbbbbb`, `neutral-text-placeholder` `#bfc7d2`, `neutral-text-ondark-deep` `#001d31`) — usa tons próprios, alguns com tinte azulado, fora da família Zinc. Confirmado nesta US via `get_variable_defs` nos nós `1427:16958`, `1421:17302`, `1421:17946`. Ver tabela em `stories/tokens/Colors.mdx`. | média | A Regra 3 declara "neutros na rampa Zinc do Tailwind" como regra geral, não só para os papéis de texto principal — a família `ui-*` em particular parece ser uma paleta técnica separada (bordas de input/conector) que talvez nunca tenha sido pensada como "neutro" pela Regra 3. Precisa de decisão humana: mapear cada papel neutro Figma para o degrau Zinc mais próximo (perdendo precisão de cor) ou abandonar a exigência de rampa Zinc pura. |
| `celule/chip/folder-tag` | ⚠️ **Suposição anterior invertida em 2026-08-09 (US-005).** A variante `isExpanded` presumida ausente (herdada de `AGENTS.md` Regra 7, escrita sem acesso ao Figma) na verdade **existe** no componente Figma real: `get_design_context` no nó `1421:19040` (achado crítico #5) retorna *"Chip de tag de pasta com estado expansível. Props: isExpanded (bool), State (Default/Hover/Selected)."* Implementada nesta US como prop `isExpanded` — comportamento visual exato 🧩 inferido (só o booleano foi confirmado, não a especificação pixel-a-pixel do estado expandido). Eixo `State=Selected` também novo, implementado como prop `selected` (estilo 🧩 inferido). | média | `AGENTS.md` (Regra 7) segue com a premissa antiga desatualizada — recomenda-se atualizar a nota da Regra 7 na próxima revisão do arquivo. Comportamento de "expandido" implementado é uma aproximação, não uma confirmação pixel-a-pixel — decisão humana bem-vinda se o produto depender de um layout expandido específico. |
| `badge/storage-tier` | ✅ **RESOLVIDO e implementado em 2026-08-10 (decisão humana, US-012)**: a versão antiga (molecule, 3 rótulos, "não aplicado a nenhuma tela") foi descontinuada e removida (`stories/molecules/StorageTierBadge.*`, `src/components/molecules/storage-tier-badge.tsx`) — a segmentação *sistêmica* "guardado"/"corrente" segue via diretórios, não badge. O componente real que o usuário criou no Figma nesse meio-tempo, `atom/StorageTierBadge` (node `1457:21014`, 2 variantes `current`/`long term`), foi implementado como átomo Figma-confirmado (`src/components/atoms/storage-tier-badge.tsx`, `stories/atoms/StorageTierBadge.*`) e passou a ser usado em `organism/cleanSpaceStorage` (rótulo por arquivo) — escopo de item individual, não navegação. Regra 6 atualizada com a distinção. | resolvida | — |
| Cor destrutiva do `atom/PushButton` (`isDestructive`) | ✅ **RESOLVIDO em 2026-08-10**: implementação inicial (mesmo dia) usava fundo+borda vermelhos inferidos. Releitura do Figma real (`organism/cleanSpaceStorage`, botões "Excluir"/"Excluir cópias") mostrou tratamento real: chrome neutro/glass igual `variant="neutral"`, só o texto na cor `var(--brand-feedback-danger-default,#bc3426)`. Corrigido em `push-button.tsx` e `--destructive` em `index.css` no mesmo dia, antes de qualquer story de US-010 rodar. | resolvida | — |
| ~20 átomos soltos no canvas (pendência histórica, resolvida para a camada atoms na US-026 pass13) | Nenhum teve descrição verificada individualmente em `figma-inventory.md` (US-002) — só nome/id/dimensão Figma-confirmados, propósito 🧩 inferido só pelo nome. Vários (`ArchiveItem`, `ImageItem`, `FolderItem`, `VideoItem`) são linhas/cards compostos, conceitualmente mais próximos do que este design system já trata como `organism` (ver `organism/file-list-item`, US-006) do que de um átomo isolado. **Atualização US-026 pass6 (2026-08-13):** `atom /VideoItem` (`1442:7858`) saiu desta pendência — `get_design_context` fresco confirmou descrição/anatomia e o átomo foi implementado com story/MDX. **Atualização US-026 pass13 (2026-08-13):** todos os átomos públicos atuais já têm `get_design_context` fresco + checklist Regra 11 registrados no manifesto da pass13 (`ArchiveItem`, `BoxIconButton`, `AddButton`, `CloseButton`, `DropdownSelect/GroupBy/Item`, `DropdownSelect/Label/Item`, `FirstUploadSymbol`, `FolderItem`, `ImageItem`, `SelectState`, `Sidebar/Tags/Items`, `StorageTierBadge`, `Switch`, `Tag`, `TagOrgMode`, `TagOrgTemplateName`, `TypeLabel`, `UploadFolder`, `VideoItem` e os átomos base anteriores). Esta linha fica como histórico de escopo, não como pendência ativa da camada atoms. | baixa | Decisão de escopo histórica, não conflito de regra travada — o nome `atom/` no Figma não segue rigorosamente a taxonomia atômica adotada neste código (ver achado #7 do inventário sobre inconsistência de prefixo `celule/`). Reabrir apenas se novos átomos soltos forem adicionados ao catálogo sem `get_design_context` próprio. |
| ~15 `molecule/`-soltos não implementados nesta US (`molecule/StorageStatus` + symbols `HeaderSelector`/`LongTerm`/`Current`/`StatusSection`/`SectionSearch`, `molecule/thumbnail-large`, `molecule/context-header`, `molecule/popover/Notification`, `molecule/template-card`, `molecule` sem sufixo (`ArchiveFileRow`/`ArchiveSelectableRow`), `molecule/nodoContextMenu`, `atom/DropdownSelect/Label/Item`, nó sem nome `1421:18687`) | Nenhum teve descrição verificada individualmente nesta US (US-005) — só nome/id/dimensão Figma-confirmados via `get_metadata` (`figma-inventory.md`, US-002), propósito 🧩 inferido só pelo nome. `template-card` (3927×424) e `StorageStatus` (1061×722)/`nodoContextMenu` seguem conceitualmente mais próximos de `organism` (composição de várias moléculas/átomos) do que de uma molécula isolada — mesmo padrão de ambiguidade de escala já registrado para os átomos soltos do canvas (linha acima). **`molecule/FolderCard`, `molecule/FileList/Header` e `molecule/FileList` saíram desta lista em 2026-08-11 (US-022)** — descrição Figma-confirmada obtida via `get_design_context` e implementados como molecule de fato (não precisaram de reclassificação para `organism`, a escala 988×1165/3804×448/1057×213 não impediu uma implementação fiel). | média | Decisão de escopo, não conflito de regra travada — implementar os ~12 componentes restantes a partir só de nome+dimensão arriscaria inventar comportamento/anatomia não confirmados (Regra 9), mesmo critério de fallback usado em `atoms` (US-004). Priorizadas até agora as moléculas com descrição Figma-confirmada obtida via `get_design_context` (`StorageBar`, `view-mode-toggle`, `action-pill`, `DropdownSelect/GroupBy`, `DropdownSelect/Label`, `FileList`, `FileList/Header`, `FolderCard`) mais as reconciliadas (`SearchBar`, `chip/folder-tag`, `badge/storage-tier`). Recomenda-se decisão humana sobre (a) buscar descrição pontual dos nós restantes antes de implementar, e (b) reclassificar os de maior escala (`template-card`, `StorageStatus`, `nodoContextMenu`) como candidatos a `organism` em vez de `molecule`, dado que o Figma real não segue rigorosamente a taxonomia atômica adotada neste código (achado #7). |
| `organism/file-list-item` (nome antigo de `molecule/FileList`), `organism/search-toolbar` | ✅ **`FileList` reconciliado com correspondência exata em 2026-08-11 (US-022)** — `get_design_context` real no nó `1421:19200` (`molecule/FileList`, descrição verbatim "estrutura de lista de aqurivo de todos os tipos") confirma que o componente **corresponde de fato** a esta linha (ícone+nome[+proprietário][+tamanho], 3 formats: `list`/`storage`/`list-sm`) — promovido a `✅ aligned`, relocado de `organisms/` para `molecules/` (nome Figma real) e reconciliado em `organism/file-list-container` (que passou a compor este molecule em vez de duplicar a marcação). A entrada anterior desta linha (US-006, sem acesso ao node certo ainda) dizia "nenhum organism formal corresponde" — desatualizada, substituída por este achado. `organism/search-toolbar` segue sem paralelo Figma confirmado, mantido 🧩 Inferido — ver nota de reconciliação em `SearchToolbar.mdx`. | baixa | `FileList` deixa de ser pendência (resolvida via leitura direta do node, não presunção). `search-toolbar` seguindo como decisão de escopo em aberto (mesma recomendação anterior: descontinuar em favor de `Header` ou manter como extensão de produto documentada, Regra 9). |
| `molecule/SearchBar` (`input/search`) | ✅ **RECONCILIADO em 2026-08-09 (US-005).** Placeholder atual do Figma confirmado verbatim: **`"Search"`** (`get_design_context` no nó `1421:17845`, `molecule/SearchBar/Placeholder/XXL`, achado crítico #1 do inventário). Implementação usa o termo aprovado "Buscar arquivos, pastas ou templates" (Regra 5) como texto correto/alvo — a substituição no Figma segue pendente. | baixa | Gap de polish (não é conflito de decisão travada) — não bloqueia nenhum fluxo ao vivo, só precisa de atualização de copy no Figma. Mantido como pendência de baixa urgência agora com o texto antigo confirmado. |
| `molecule/SearchBar` (`1421:17857`) e `molecule/InputStates` (`1518:7924`) | ✅ **RESOLVIDO em 2026-08-14 — decisão humana.** `molecule/InputStates` é peça nova, real, criada pelo usuário no Figma (não referência colada) — o usuário percebeu a carência de um input com feedback de validação e segregou essa peça de propósito. Implementado como novos estados `state="success"\|"danger"` em `search-input.tsx` (não um componente novo — arquitetura de extensão do `SearchInput` existente, mesma anatomia). Escopo restrito às 2 variantes reais desse node (`writeValue`/sucesso, `wrongValue`/erro); os eixos `Focused`/`Typing`/`ValueIdle`/`ValueDisable` do `SearchBar` vizinho ficam de fora (decisão humana — peça diferente). Por decisão humana, só a fonte do placeholder foi adaptada pra Figtree (já era o padrão do componente); ícone segue `SearchIcon` do lucide-react já em uso (não o glifo SF Symbol do node, sem asset exportável). Anel de sucesso reaproveita `var(--brand-feedback-success-default,#096)` (mesmo token já usado em `celule/TagColor`); anel de erro reaproveita `--destructive` (`#bc3426`, já usado no projeto todo) em vez do `#c0392b` literal deste node — mesma categoria de pequena divergência de variável já documentada para outros nós de "perigo" nesta tabela. Ver `stories/molecules/SearchInput.mdx`. | resolvida | — |
| `celule/chip/folder-tag` | ✅ **RECONCILIADO em 2026-08-09 (US-005).** Camada com `opacity:0` residual confirmada e localizada: texto **"Pessoal"** no estado `Default`/não-expandido (`get_design_context` no nó `1421:19040`, achado crítico #6). Não reproduzido no componente — o chip não exibe texto oculto/residual. | baixa | Gap de limpeza do arquivo Figma fonte, já confirmado e localizado — não afeta o comportamento do componente implementado (`stories/molecules/FolderTagChip.mdx`). |
| `atom/PushButton` | Estados `Loading`/`Error` implementados no código **não existem** no enum `State` confirmado do Figma (`Idle`\|`Hover`\|`Clicked`\|`Disabled` apenas, achado do inventário US-002). Eixo `Active Window` (True/False, Figma-confirmado) também não é implementado — não confirmado como aplicável a uma app web (padrão desktop/foco de janela). | baixa | Gap de engenharia, não conflito de decisão — `Loading`/`Error` são mantidos por necessidade real de produto (ações assíncronas/validação em "Guardar"), documentados como extensão não Figma-confirmada em `stories/atoms/PushButton.mdx`, nunca apresentados como fato do Figma (Regra 9). |
| `atom/PushButton` (`1421:17302`, eixo `Style`) | 🆕 **Achado novo em 2026-08-13 (US-026 pass18).** `get_design_context` fresco confirma que o enum `Style` do component set Figma tem 7 valores: `Bordered Colored`, `Bordered Destructive`, `Bordered Neutral`, `Bordered Secondary`, `Borderless`, `Borderless (Bezel shows On)`, `Default`. `push-button.tsx` implementa só 2 (`variant="primary"` = Bordered Colored, `variant="neutral"` = Bordered Neutral); `Bordered Destructive`/`Bordered Secondary`/`Borderless`/`Borderless (Bezel)`/`Default` seguem não implementados — já mencionado em comentário de código (`push-button.tsx`) como "decisão de produto fora do escopo atual", mas nunca antes registrado nesta tabela. | baixa | Regra 1 exige um único componente de botão (não variantes como componentes separados) — `PushButton` continua sendo esse componente único, e os 2 estilos implementados cobrem os fluxos ao vivo reais auditados (Guardar/Cancelar/Excluir). Não é violação de regra travada, é cobertura parcial de um enum de estilo com 7 valores; recomenda-se decisão humana só se um fluxo real precisar de `Bordered Destructive`/`Bordered Secondary`/`Borderless` no futuro. |
| `atom/DeleteButton`, `atom/PlusButton`, `atom/ActionButton/Confirm`, `atom/ClearButton`, `atom/KeepButton` | `state` Figma-confirmado inclui `On`/`Idle` como valores distintos do enum, sem nenhuma descrição encontrada que diferencie o comportamento dos dois; `atom/ClearButton` tem ainda um sexto valor (`ClickedFilled`) sem descrição própria distinguindo-o de `Clicked`. | baixa | Gap de documentação do próprio Figma (não do design system) — implementar uma distinção visual inventada entre `On`/`Idle` ou `Clicked`/`ClickedFilled` violaria a Regra 9. Os 5 átomos tratam os pares como o mesmo estado de repouso/press até confirmação futura, registrado em cada `.mdx` (`stories/atoms/`). |
| `atom/Icon` (44+ instâncias, seção Icon/) | ✅ **RESOLVIDO (achado em US-026).** Esta entrada estava desatualizada: uma US anterior (não numerada nesta tabela) já substituiu os glifos `lucide-react` por vetores reais exportados do Figma via `download_assets` — `src/components/atoms/icon.tsx` importa hoje ~100 SVGs reais de `src/assets/icons/`, não aproximações por nome/significado. A auditoria de ponto-fixo (US-026) spot-checou `Arquivar`/`Folder`/`ArrowBack` pixel-a-pixel contra os SVGs exportados do Figma e confirmou o match; verificação exaustiva dos 44+ glifos individualmente segue fora do orçamento de uma única passada (mesma lógica de amostragem de `unused.mdx`), mas a premissa original desta entrada (glifos genéricos) não é mais verdadeira. | resolvida | — |
| `organism/storage-sidebar` (tags de tier) | 🆕 **Achado novo em 2026-08-10 (US-006).** A tag Figma-confirmada nesta variante do widget é **"Corrente"**, divergindo do termo aprovado "Acesso rápido" (Regra 5) usado na variante embutida em `organism/Sidebar` (mesmo conceito, grafias diferentes no mesmo arquivo Figma). Rótulos dos botões "Gerir Espaço"/"Comprar Espaço" também não constam na lista aprovada da Regra 5 (nem são proibidos). | baixa | Gap de terminologia, não termo proibido — `StorageSidebar.tsx` usa "Acesso rápido"/"Longo prazo" (termos aprovados) para consistência, registrado como divergência interna do Figma a resolver eventualmente na fonte. |
| `organism/upload-popover` | 🆕 **Achado novo em 2026-08-10 (US-006).** Todo o texto Figma-confirmado deste nó (`1421:19292`) está em inglês ("Uploading 3 files", "6 seconds left", "IN PROGRESS", "62% • 18MB OF 29MB") — inconsistente com o restante da página, que é em português. `UploadPopover.tsx` usa tradução direta ("Enviando N arquivos" etc.), nunca o texto literal do Figma (Regra 9). | baixa | Gap de consistência de idioma do próprio arquivo Figma, não decisão de design — não bloqueia a implementação (tradução direta e não ambígua), só precisa de correção de copy na fonte eventualmente. |
| `organism/Header` (`page=Navbar`) | 🆕 **Achado novo em 2026-08-10 (US-006).** O botão "Organizar" (Figma-confirmado literalmente no código-fonte do nó `1421:19918`) não está na lista de terminologia aprovada da Regra 5 (que lista só "Acesso rápido", "Longo prazo", "Guardar", "Arquivar", "Pronto para guardar", "Ver duplicados", "Buscar arquivos, pastas ou templates"), nem é termo proibido. | baixa | Gap de cobertura da lista travada (Regra 5 provavelmente não previu o fluxo "Organização" ao ser escrita) — "Organizar" é consistente com o domínio já descrito em `AGENTS.md` e com as telas "Organização"/"Organização - Modo Data" do inventário Figma. Recomenda-se adicionar "Organizar" à lista aprovada numa revisão futura de `AGENTS.md`, não é uma violação. |
| `atom/Icon/SpatialAudioOff` | 🆕 **Achado novo em 2026-08-11.** Vetor real exportado do Figma (`download_assets`, nó `1421:17814`) para esse nome de camada é um ícone de **pessoa/avatar** (círculo com cabeça+ombros), não um ícone de "mudo"/áudio desligado como o nome sugere. Implementado com o vetor real e literal (Regra 9 — não silenciosamente substituído por um ícone de mudo mais "lógico"), usado em `molecule/ActionPill` como ação "Silenciar". | baixa | Divergência entre nome da camada e conteúdo visual no próprio arquivo Figma (provável reaproveitamento de ícone) — não é erro de implementação. Recomenda-se confirmar com quem mantém o Figma se o ícone certo foi anexado a esse nó. |
| `molecule/nodoContextMenu` (toggle "E"/"OU") | 🆕 **Achado novo em 2026-08-11 (US-016).** `get_design_context` no nó `1440:23821` confirma o estado selecionado do toggle lógico "E"/"OU" com um hex bruto (`bg-[#92ccff]`), sem variável semântica de tema (viola Regra 2, "nunca nomes que vazam implementação"/cor hardcoded fora do tema). Implementado com `brand-teal` (mesmo tratamento de "selecionado" já usado em `DropdownSelectGroupBy`/`ViewModeToggle`), não o hex literal. | baixa | Elemento pequeno (2 estados de um toggle interno) dentro de um componente maior recém-implementado — não bloqueia nenhum fluxo ao vivo; recomenda-se o Figma mapear esse hex para um token semântico numa revisão futura. |
| `organism/planSelection` (`1454:25057`) | ✅ **RESOLVIDO em 2026-08-18 (decisão humana).** Achado original (2026-08-11, US-025): a Regra 5 presumia que Configurações de Plano teria um botão "Liberar Espaço" — `get_design_context` confirmou que não tem. Esclarecido pelo usuário: a conexão real é o botão "Comprar Espaço" de `molecule/StorageStatus` (`scope="global"`), que leva de Armazenamento pra Configurações de Plano — não o contrário. O botão do rodapé de `PlanSelection` foi renomeado no Figma pelo usuário, ao vivo, de "Editar pagamento" pra **"Editar plano"** (node `1454:25054`, releitura via `get_metadata` confirma) — atualizado em `plan-selection.tsx`. Regra 5 corrigida com a direção certa. | resolvida | — |
| `organism/planSelection` (`Rectangle 20`, `1454:25055`) | 🆕 **Achado novo em 2026-08-11 (US-025).** `get_metadata` confirma um retângulo de 19×12px só com borda (`var(--brand-secondary-light,#6b6b68)`), sem preenchimento/texto/ícone, solto direto no card raiz (não filho de nenhuma seção nomeada), posicionado colado à borda direita do card e parcialmente cortado por ela. Sem descrição, sem propósito visual reconhecível. Não implementado (Regra 11.4 — nunca inventar elemento não confirmável). | baixa | Mesmo padrão de artefato residual já visto em `celule/chip/folder-tag` (camada `opacity:0`) — provável leftover de edição no arquivo Figma fonte, não elemento de produto. Recomenda-se o Figma remover essa camada numa revisão futura. |
| `organism/planSelection` (rodapé, textos em inglês) | 🆕 **Achado novo em 2026-08-11 (US-025).** Vários textos Figma-confirmados estão em inglês em uma tela majoritariamente em português: "Active", "Next billing on Jul 8, 2026", "Monthly"/"Annual", "Current", "✓ Active", "Downgrade"/"Upgrade" — e o texto do rodapé mistura os 2 idiomas na mesma frase: *"**Update** método de pagamento, visualize ordens de pagamento, ou cancele no portal da Stripe"*. Implementado inteiramente traduzido para português (mesmo critério já usado em `organism/upload-popover`), nunca o texto literal do Figma (Regra 9). | baixa | Gap de idioma do próprio arquivo Figma fonte, mesmo padrão já registrado para `organism/upload-popover` e `organism/Header` — não bloqueia nenhum fluxo, só precisa de correção de copy na fonte eventualmente. |
| `organism/FAQ/FastLinks` (`1454:25006`, rótulos dos 3 links) | 🆕 **Achado novo em 2026-08-11 (US-025).** Os 3 rótulos Figma-confirmados ("API Documentation", "Community Forum", "Video Tutorials") estão em inglês, enquanto o restante da aba de FAQ (`FaqInfoCard`, `FaqInfoCardCollapsed`) é inteiramente em português. Implementado com tradução direta ("Documentação da API", "Fórum da comunidade", "Tutoriais em vídeo"), mesmo critério de `upload-popover`. Nenhum dos 3 rótulos está na lista proibida/aprovada da Regra 5. | baixa | Gap de idioma do próprio Figma, não decisão de design — não bloqueia nenhum fluxo, copy consistente e não ambígua. |
| `organism/FAQ/FastLinks` (`1454:25006`, fonte dos links) | 🆕 **Achado novo em 2026-08-11 (US-025).** `get_design_context` confirma `font-['Manrope:Medium']` nos 3 rótulos de link (título "Links Rápidos" já usa Figtree corretamente) — mesmo gap de fonte já visto em `molecule/context-header` (Regra 4 trava Figtree como tipografia única). Implementado com Figtree (`font-sans`, padrão do projeto), não Manrope. | baixa | Mesmo padrão já registrado para `molecule/context-header` — 3 rótulos de link, não um fluxo ao vivo crítico; risco de exposição baixo. |
| `organism/FAQ/FastLinks` (`1454:25006`, padding do frame raiz) | ✅ **RESOLVIDO em 2026-08-14 (US-026 pass20).** `get_design_context` fresco confirma que o frame raiz tem só `py-[24px]`, sem padding horizontal (`px`) — uma correção anterior (pass14, 2026-08-13) havia adicionado `px-4` por engano, sem correspondência no Figma. Isso estreitava a área de texto disponível o bastante pra fazer as traduções PT (mais longas que os rótulos originais em inglês) quebrarem em 2 linhas. Removido o `px-4` em `faq-fast-links.tsx`; recapturado em `organisms-faqfastlinks--default.png`, os 3 links agora ficam numa linha só. | resolvida | — |
| `organism/OrganizeFreeModeCanvas` (`1439:16906`) | ✅ **RESOLVIDO em 2026-08-13 (US-026 pass16).** Achado original de 2026-08-11: `get_design_context` completo confirmou conectores tracejados e painel flutuante `molecule/contextMenu/FreeMode` ainda não implementados. Pass16 reabriu o node e também encontrou a simplificação estrutural restante: a story ainda usava uma cadeia flex, enquanto o Figma atual confirma composição absoluta 1117×933 com source group, filtros empilhados, `Junção`, `Auto-Archive` abaixo, `Resultado` expandido, painel, minimap e footer. Corrigido em `organize-free-mode-canvas.tsx` e recapturado em `.audit-artifacts/us-026-active/screenshots-pass16/organisms-organizefreemodecanvas--default.png`. | resolvida | — |
| `molecule/DropdownSelect/Label` (`1439:19650`) | 🆕 **Achado novo em 2026-08-12 (US-026, 2ª auditoria de ponto-fixo).** `get_design_context` retorna `opacity-[var(--radius-3xl,0.32)]` aplicado ao componente inteiro nessa instância única do Figma (sem component set/eixo de estado) — um nome de variável de *radius* usado, por engano aparente no arquivo fonte, para controlar *opacity*, deixando a instância a ~32% de opacidade no próprio canvas. Não reproduzido: o componente implementado renderiza em opacidade plena por padrão, só esmaecendo via a prop `disabled` já existente (`opacity-50`). Ver `stories/molecules/DropdownSelectLabel.mdx`. | baixa | Não fica claro se os 32% do node único são a aparência-padrão pretendida ou um artefato de como essa instância foi deixada no canvas Figma (Regra 9) — mudar por inferência arriscaria fazer o componente nascer com aparência de desabilitado sem necessidade real. Recomenda-se confirmar no Figma se a variável certa era opacity ou se é mesmo um erro de nomenclatura de token na fonte. |
| `molecule/nodoContextMenu` (`atom/buttonAdd`, rótulo) | 🆕 **Achado novo em 2026-08-12 (US-026, 2ª auditoria de ponto-fixo).** `get_design_context` no nó `1440:23821` retorna o texto literal do `atom/buttonAdd` como **"Label"**, tanto no código de referência quanto no screenshot do próprio Figma — mas a descrição semântica do componente confirma o propósito real ("adicionar novas regras de filtro no menu contextual"). Implementado com "Adicionar Regra" (não o literal "Label", que parece um placeholder/bug de preenchimento no arquivo Figma fonte), mesmo critério já usado em `molecule/radiobutton` (US-013, texto real via nome do node em vez de extração com bug). Ver `stories/molecules/NodeContextMenu.mdx`. | baixa | Não bloqueia nenhum fluxo — "Adicionar Regra" é a leitura mais coerente com o propósito confirmado do componente; "Label" no Figma tem cheiro forte de placeholder esquecido. Recomenda-se o Figma corrigir o texto literal do botão numa revisão futura. |
| `celule/MainCanvas/Organization/FreeMode/ItemNode` (nós de filtro, `1439:16906`) | ✅ **RESOLVIDO em 2026-08-13 (US-026, 4ª passada).** Achado original: `get_design_context` confirmou texto completo Figma-confirmado nos 2 nós de filtro do canvas de Organização Livre — **"Filtro: Grande"** (`Size &gt; 1.0 GB`) e **"Filtro: Formato"** (`Type = .mp4, .mov`) — enquanto o organism renderizava só os defaults curtos do celule base (`"Filtro"` + `"Size "`/`"Type"`). Causa raiz preservada: o próprio `celule/FreeMode/ItemNode` (`1421:20108`) estava correto; os textos completos pertencem à instância composta em `organism/OrganizeFreeModeCanvas` (`1439:16906`). Correção: `FreeModeItemNode` agora aceita overrides opcionais `label`/`subtitle`, e `OrganizeFreeModeCanvas` repassa os dois textos específicos. | resolvida | — |
| `molecule/FileList` / `organism/FileListContainer` (ícone da linha "Arquivo 2") | 🆕 **Achado novo em 2026-08-12 (US-026, 2ª auditoria de ponto-fixo).** Inconsistência do lado da ferramenta MCP do Figma, não do código: o `get_design_context` no nó `1421:19687` retorna o mesmo asset SVG reusado nas 3 linhas de exemplo, mas o `get_screenshot` do mesmo nó mostra um ícone visualmente diferente na 2ª linha ("Arquivo 2"). Implementação seguiu o código literal retornado (mesmo ícone nas 3 linhas), Regra 9 — não inventada uma diferenciação sem confirmação inequívoca de qual ícone é o correto. | baixa | Não bloqueia nenhum fluxo — é dado de exemplo (nome de arquivo fictício), não estado de produto. Recomenda-se checar diretamente no arquivo Figma (fora do MCP) qual ícone é o real da 2ª linha, já que código-fonte e screenshot da mesma chamada divergem entre si. |
| `organism/PreviewPane` (seção "Etiquetas", tag "Urgente") vs. Regra 8/11 (cor de perigo) | 🆕 **Achado novo em 2026-08-12 (US-026, 3ª auditoria de ponto-fixo).** `get_design_context` no nó `1421:19405` confirma que a tag "Urgente" em "Etiquetas" é um badge **preenchido** em vermelho (`#bc3426`, texto branco) — não chrome neutro com texto vermelho. Implementado literal (Regra 9) usando o átomo já existente `DangerTypeLabel`, que reproduz fielmente esse preenchimento. Isso contradiz a leitura geral desta mesma auditoria (Regra 3/11: "cor de perigo é chrome neutro/glass com só o texto nessa cor, nunca fundo vermelho preenchido") — leitura derivada especificamente dos botões "Excluir"/"Excluir cópias" de `organism/cleanSpaceStorage`. Nenhuma das duas leituras foi alterada por inferência; ambas são Figma-confirmadas em nós diferentes. Ver `stories/organisms/PreviewPane.mdx`. | média | O Figma real parece usar 2 tratamentos distintos para "perigo" dependendo do contexto (botão de ação destrutiva = texto only; badge/tag de status = preenchido) — não necessariamente uma inconsistência do arquivo fonte, pode ser intencional. Precisa de decisão humana sobre se a Regra 3/11 deve documentar os 2 tratamentos como válidos por contexto (ação vs. rótulo de status) ou se um dos dois nós está desatualizado. |
| `celule/cleanSpaceStorage/listSelection` (ícone de arquivo, cor) | ✅ **REVERIFICADO em 2026-08-12 (US-026, 3ª auditoria de ponto-fixo) — confirmado azul, não teal.** Um dos lotes desta passada relatou uma leitura conflitante (`organism/cleanSpaceStorage` sugerindo teal `#c8dce3` para esse ícone); tie-break feito com `get_screenshot` direto no nó `1439:16908` (`organism/cleanSpaceStorage` completo) confirma visualmente que os 3 ícones de arquivo em "Arquivos grandes" são azuis, consistentes com `#2b7fff`/`rgba(43,127,255,0.1)` já implementado em `clean-space-list-selection.tsx`. Nenhuma mudança de código necessária — a leitura "teal" do outro lote foi descartada como erro de leitura pontual, não um conflito real do Figma. | resolvida | — |
| (infra) Cobertura de teste automatizado | 🆕 **Registrado em 2026-08-18 (avaliação de handoff).** O gate real é só `tsc --noEmit` + `build-storybook` (`scripts/gate.sh`) — não existe teste unitário nem de regressão visual automatizado. `@storybook/addon-vitest`/Playwright estão instalados como devDependency, nunca wired. Uma versão anterior de `AGENTS.md` ("Critérios mínimos de aceitação") citava "testes unitários da story passam"/"sem regressão nos testes existentes" como se fossem parte do gate real — não eram, corrigido no mesmo commit deste registro. | baixa | Não bloqueia handoff — mas é débito que precisa ser explícito pro time que recebe, não descoberto depois. Recomenda-se a engenharia decidir se ativa `addon-vitest` (interaction tests) e/ou regressão visual (Chromatic ou Playwright direto contra `storybook-static/`) como parte da integração do design system ao pipeline de CI do produto. |
| (infra) Acesso ao Figma | ✅ **RESOLVIDO em 2026-08-09 (US-003).** O `fileKey` (`oFp2TLeCG4GJeCOFVhBvjg`) e o nodeId da página (`1421:17272`) foram persistidos em `AGENTS.md` (seção "Fonte Figma"), e o MCP do Figma respondeu `get_metadata`/`get_design_context`/`get_variable_defs` normalmente nesta sessão — `design-system/docs/figma-inventory.md` (US-002) já existe e foi usado para reconciliar `stories/tokens/*.mdx` nesta US. Mantendo a linha por histórico; não é mais um bloqueio ativo. | resolvida | — |
