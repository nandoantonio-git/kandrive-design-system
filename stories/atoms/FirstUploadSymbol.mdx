import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as FirstUploadSymbolStories from './FirstUploadSymbol.stories';

<Meta of={FirstUploadSymbolStories} />

# FirstUploadSymbol (`atom/firstUploadSymbol`)

**Status: ✅ aligned (estrutura)** — Figma-confirmado nesta US (US-013,
releitura pontual de 2026-08-10), nó `1454:20974`, descrição verbatim:
"símbolo de upload para o kickoff inicial do usuário na plataforma."

🔧 **Corrigido em auditoria US-026 (2026-08-11):** o glifo estava a
`size-16` (64px, 24% do diâmetro) — `get_design_context` confirma
`aspect-[12/12] left-[33.9%] right-[34.27%]`, ou seja ~85px (32% do
diâmetro de 267px). Corrigido para `size-[85px]`.

**Reverificado na 3ª passada de auditoria de ponto-fixo (US-026,
2026-08-12):** node re-lido do zero via `get_design_context` (obrigatório
por ter sido editado pelo usuário no Figma em 2026-08-10) — descrição
verbatim e composição (círculo `#D9D9D9` + `atom/Icon/CloudDownload`
85px) permanecem idênticas ao já documentado; nenhuma divergência nova.

<Canvas of={FirstUploadSymbolStories.Default} />
<Controls />

## Anatomia (Figma-confirmado)

267×267px: círculo de fundo (`atom/firstUploadSymbol`, asset de imagem) +
`atom/Icon/CloudDownload` sobreposto, centralizado, ~1/3 inferior do
círculo. Sem props — elemento puramente decorativo, sem variantes.

## Fidelidade code-level (gap documentado, Regra 8/9)

O círculo de fundo é exportado pelo Figma como **asset de imagem**, não como
forma vetorial com cor/gradiente inline. A implementação reproduz a estrutura
confirmada no screenshot Figma e na releitura US-026 pass13: círculo sólido
`#D9D9D9` em 267×267 e glifo `CloudDownload` `#6B6B68` em 85×85, centralizado.
O asset exato segue documentado como gap de fidelidade pixel-a-pixel por não
estar incorporado ao código como imagem exportada.

## Uso pretendido

Estado vazio / onboarding inicial (empty state de "nenhum arquivo ainda") —
inferido pela descrição Figma ("kickoff inicial"), sem tela específica do
inventário confirmando o contexto exato de uso (🧩 inferido, Regra 9).

## Estados (Regra 8)

| Estado | Aplicável? | Nota |
| --- | --- | --- |
| Default | ✅ | Único tratamento — elemento estático |
| Hover/Active/Disabled/Loading/Error | ❌ Não aplicável | Símbolo decorativo, não é um controle interativo |

## Fluid interface (Regra 8)

Não aplicável — sem interação, sem transição. **reduced-motion: não
documentado no Figma** (nenhuma variante/nota encontrada no nó consultado).

## Material Liquid Glass

Não aplicável — preenchimento sólido, sem efeito de vidro (Regra 10).
