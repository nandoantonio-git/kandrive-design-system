import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as ConfirmButtonStories from './ConfirmButton.stories';

<Meta of={ConfirmButtonStories} />

# ConfirmButton (`atom/ActionButton/Confirm`)

## Uso

Ícone-botão sem chrome de borda pra **confirmar** uma ação (ex.: aceitar uma sugestão, validar um campo). Par funcional do `ClearButton` — normalmente aparecem juntos (confirmar/cancelar) num mesmo contexto de edição inline.

<Canvas of={ConfirmButtonStories.Default} />
<Controls />

## Variantes de cor

`style`: `Default` · `Primary` · `White`.

<Canvas of={ConfirmButtonStories.Primary} />
<Canvas of={ConfirmButtonStories.White} />
<Canvas of={ConfirmButtonStories.Disabled} />

## Estados

`Idle` · `Clicked` (feedback via `scale-90`) · `Disabled`. Sem loading/error.

## Terminologia

Não renderiza texto visível — `aria-label="Confirmar"`.
