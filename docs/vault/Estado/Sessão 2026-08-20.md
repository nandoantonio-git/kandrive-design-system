---
tags: [estado, log]
---

# Sessão 2026-08-20

Sessão interativa, fora do [[Ralph Loop]]. Usuário atualizou elementos e hierarquia no Figma fonte e trouxe uma lista de links pra revisão, pedindo (1) reconciliação da migração `celule`→`molecule` já feita no código (ver nota histórica em [[Camadas Atômicas]]), (2) criação de uma camada `templates` + `pages` no Storybook.

## 1. `celule` → `molecule`: confirmado no Figma

`get_metadata` fresco em 2 nodes citados pelo usuário confirma o rename já propagado na fonte: `molecule/cleanSpaceStorage/listSelection` (`1436:20496`, era `celule/...`) e `molecule/Menuitem/freemodeOrganization` (`1431:20042`, antes um node solto sem prefixo semântico). Ver achado sobre a peça `Buttons` ainda não renomeada, item 4.

## 2. Achado real: camada `template/` nova no Figma

Não era só um rename de `celule` — 6 nodes que eram `organism/*` viraram `template/*` no Figma fonte, mesmos nodeIds:

| Componente | Figma antes | Figma agora | nodeId |
| --- | --- | --- | --- |
| CleanSpaceStorage | `organism/cleanSpaceStorage` | `template/cleanSpaceStorage` | `1439:16908` |
| ArchiveBrowserModal | `organism/ArchiveBrowserModal` | `template/ArchiveBrowserModal` | `1439:16909` |
| SaveLongTermFileStorage | `organism/Save/LongTermFileStorage` | `template/Save/LongTermFileStorage` | `1439:16907` |
| OrganizeFreeModeCanvas | `organism/MainCanvas/Organization/FreeMode` | `template/MainCanvas/Organization/FreeMode` | `1439:16906` |
| SaveOrganizationModal | `organism/DialogSave/OrganizationModal` | `template/DialogSave/OrganizationModal` | `1421:18576` |
| TemplateReviewModal | `organism/Dialog/TemplateReviewModal` | `template/Dialog/TemplateReviewModal` | `1431:20397` |

Todos os 6 promovidos: `src/components/organisms/*.tsx` → `src/components/templates/*.tsx`, stories `Organisms/*` → `Templates/*`, doc-comments atualizados com nota datada da reclassificação (Regra 9 — histórico preservado, não reescrito silenciosamente).

## 3. 5 sub-componentes extraídos como organisms próprios

O usuário também deu symbol Figma próprio a 5 seções que antes só existiam como markup inline dentro dos templates acima — extraídas como organisms de fato (Regra 10, nunca reimplementar):

- `CleanSpaceLargeFiles` (`organism/cleanSpaceStorage/LargeFiles`, `1554:21264`)
- `CleanSpaceDuplicated` (`organism/cleanSpaceStorage/Duplicated`, `1554:21265`)
- `TemplateReviewModalItem` (`organism/Dialog/TemplateReviewModal/Item`, `1554:21151`)
- `ArchiveBrowserModalSidebar` (`organism/ArchiveBrowserModal/sidebar`, `1555:21309`)
- `SaveLongTermFileStorageSelectedFiles` (`organism/Save/LongTermFileStorage/SelectedFiles`, `1555:21357`)

Reconciliadas via `get_metadata` (texto/estrutura batem 100% com o que já estava implementado e auditado nos templates pai) — sem `get_design_context` extra por não ter sido encontrada nenhuma diferença visual/textual (Regra 9, custo-benefício documentado em cada `.mdx`).

Mais 1 molecule nova: `FreeModeAddMenu` (`molecule/Menuitem/freemodeOrganization`, `1431:20042`) — dropup do canvas Modo Livre, descrição Figma verbatim confirmada via `get_design_context` real. Achado de fidelidade **não corrigido**: o node sugere painel mais estreito (228px) e translúcido/Liquid Glass, contra os 256px/fundo sólido já implementados — registrado em [[Conflitos Abertos]] (fonte completa em `making-of/conflicts.md`), decisão humana pendente.

## 4. Achado: 1 peça `celule` ainda não renomeada

Das 10 peças da extinta camada `Cells`, `celule/MainCanvas/Organization/FreeMode/Buttons` (`1431:20043`) é a única que **ainda não** foi renomeada `molecule/` no Figma fonte — confirmado via `get_metadata` direto no symbol (não só na instância). Código já trata como `molecule` (`FreeModeButtons`) desde a extinção da camada; só o Figma precisa da correção manual. Registrado em [[Conflitos Abertos]] (fonte completa em `making-of/conflicts.md`), não corrigido (não é decisão de código).

## 5. Contagem atualizada

29 atoms + 33 molecules (+1) + 22 organisms (-6 promovidos, +5 extraídos) + 6 templates (camada nova) = **90 componentes**, 4 camadas. Ver [[Camadas Atômicas]].

## 6. "Pages": recon + primeiras 4 telas implementadas

Recon do node "Pages" (`1439:25740`, 23 telas) via `get_metadata` (dump de 200KB, processado localmente com `grep`/`awk`, nunca carregado inteiro no contexto). Achados: nenhuma tela adicionada/removida desde 2026-08-09; quase todas ganharam prefixo `page/*` (2 exceções de nomenclatura, ver [[Conflitos Abertos]]); composição interna estável — todo componente referenciado já existe no catálogo, nenhuma peça nova descoberta; os 6 templates promovidos hoje não aparecem como instância direta em nenhuma das 23 telas (só via overlay de modal, sem conflito de referência).

Priorizado com o usuário: as 4 telas Home/Organização primeiro (`page/Home/GridMode`, `page/Home/ListMode`, `page/Home/Columns Mode/Item1`, `page/Organização`). Implementadas como **2 componentes** (não 4) — `HomePage` cobre os 3 nodes `Home/*` via prop `viewMode` (mesmo critério de eixo único já usado em `ViewModeToggle`), `OrganizationPage` reusa o mesmo shell com 2 diferenças confirmadas (sem breadcrumb, título sem legenda) + o modal `SaveOrganizationModal` em overlay. Nenhum componente novo de atoms/molecules/organisms/templates foi necessário — só 1 molecule de suporte nova, `Breadcrumb` (sem node Figma próprio).

Verificado com screenshot real (Playwright contra `storybook-static/` servido localmente, não só build sem erro) comparado contra os screenshots do Figma capturados na mesma sessão — composição bate, nenhuma divergência material. As outras 19 telas ficam pra uma rodada futura.

## Ver também

- [[Camadas Atômicas]]
- [[Regra 11 - Protocolo de Verificação]]
- [[Backlog (User Stories)]]
