import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as HeaderStories from './Header.stories';

<Meta of={HeaderStories} />

# Header (`organism/Header`)

## Uso

Cabeçalho fixo do topo — 3 variantes por página (`Navbar`/`settings`/`storage`). Só `Navbar` expõe as ações de fluxo ao vivo (Organizar/Guardar); use `settings`/`storage` nas páginas correspondentes, sem essas ações.


**Status: 🔧 corrigido em 2026-08-13 (US-026, 12ª passada ativa)** ·
**⚠️ gap de terminologia aberto — urgência baixa** (ver seção abaixo e
`docs/conflicts.md`) — Figma-confirmado, nó `1421:19918`, descrição
verbatim: *"header"*. 3 variantes Figma-confirmadas (`page=Navbar`\|
`settings`\|`storage`) — só `Navbar` expõe as 2 ações de fluxo ao vivo
"Organizar"/"Guardar" (Figma-confirmadas literalmente no código-fonte do
nó).

🔧 **Achado novo nesta releitura:** os botões "Organizar"/"Guardar"
sobrescreviam `atom/PushButton` com `className="h-9 px-3 text-sm"`.
`get_design_context` fresco confirma a linha de botões a `h-[40px]` com
`padding-inline: 16px` — exatamente os defaults do átomo (`h-10 px-4
text-base`), não o override. O `text-sm` (14px) também furava
silenciosamente o piso de acessibilidade de 16px já travado para rótulo de
botão em `atom/PushButton` (US-011). `Header` não estava na lista de 8
organisms com esse gap já catalogado como conhecido/deferido em
`PushButton.mdx` — achado genuinamente novo, corrigido removendo o
`className` para herdar os defaults corretos do átomo.

🔧 **Achado material adicional nesta retomada:** o screenshot Storybook
mostrou o header com altura ~72px, logo menor e `action-pill` deslocada
para perto da busca nas variantes `settings`/`storage`; o Figma fresco
confirma frame de 96px, logo `173×44`, busca `560px`, borda inferior
`neutral-border-default` e `action-pill` alinhada à direita. Corrigido em
`header.tsx` com `h-24`, `py-6`, logo `h-11 w-[173px]`, busca fixa
`w-[560px]` e `ml-auto` na pílula.

<Canvas of={HeaderStories.Navbar} />
<Controls />

## ⚠️ Gap de terminologia — Regra 5, urgência baixa (ver `docs/conflicts.md`)

"Organizar" não está na lista de terminologia aprovada (Regra 5: "Acesso
rápido", "Longo prazo", "Guardar", "Arquivar", "Pronto para guardar", "Ver
duplicados", "Buscar arquivos, pastas ou templates") nem na lista
proibida. É Figma-confirmado literalmente e consistente com o domínio já
descrito em `AGENTS.md` ("fluxos reais... Organização"/Pages) — registrado
como gap de cobertura da lista travada, não como violação.

O placeholder de busca "Pesquisar" (Figma-confirmado neste nó) diverge do
placeholder "Search" já registrado para `molecule/SearchBar/Placeholder/XXL`
em `SearchInput.mdx` — mesma família de inconsistência interna do Figma
(2 placeholders distintos para o mesmo componente em telas diferentes),
não uma decisão nova.

## Composição (Regra 10)

| Peça | Origem |
| --- | --- |
| `molecule/input-search` | `Molecules/SearchInput` (US-005) |
| `atom/PushButton` (`variant="primary"`) | `Atoms/PushButton` |
| `molecule/action-pill` | `Molecules/ActionPill` (US-005) — composição real Help/Settings/Conta (corrigida em 2026-08-11, ver `ActionPill.mdx`; a menção anterior a "2×Settings + SpatialAudioOff" nesta linha estava desatualizada) |

## Conflito de variantes (Regra 1)

"Organizar" e "Guardar" usam `PushButton variant="primary"` — nenhum
componente `button/primary`\|`secondary`\|`destructive` separado. "Guardar"
é termo aprovado; "Organizar" ver gap acima.

## Estados e fluid interface (Regra 8)

Herdados dos sub-componentes (`SearchInput`, `PushButton`, `ActionPill`) —
nenhum estado próprio do container. **reduced-motion**: não documentado no
Figma; nenhuma animação própria do `Header`.

## Material Liquid Glass

O `action-pill` embutido usa Liquid Glass — já documentado em
`ActionPill.mdx`/`Tokens/Materials`; o `Header` em si (fundo
`bg-zinc-50`) não usa o material.

🔧 **Corrigido em 2026-08-11 (auditoria fixed-point, Regra 11):** a
`action-pill` do `Header` é Figma-confirmada a `opacity: 50%` nas 3
variantes `page` — implementação renderizava 100% opaca. Corrigido com
`className="opacity-50"` em `header.tsx` (só o efeito visual, sem acionar a
prop `disabled` do átomo).

## Terminologia

"Guardar" ✅ aprovado. "Organizar" ver gap acima. Nenhum termo proibido
encontrado.
