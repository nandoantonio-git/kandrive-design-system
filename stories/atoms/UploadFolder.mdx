import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as UploadFolderStories from './UploadFolder.stories';

<Meta of={UploadFolderStories} />

# UploadFolder (Figma: `atom/UploadFolder`, `1439:17053`)

## Uso

Ícone de upload de uma pasta — glifo composto (contorno de pasta + seta de upload sobreposta), 16×16px. Use em qualquer ação de upload que seja especificamente de uma pasta inteira, não de um arquivo avulso.

<Canvas of={UploadFolderStories.Default} />
<Controls />

## Estados

Glifo estático — sem hover/pressed/disabled próprios.
