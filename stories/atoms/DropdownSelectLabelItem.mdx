import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as DropdownSelectLabelItemStories from './DropdownSelectLabelItem.stories';

<Meta of={DropdownSelectLabelItemStories} />

# DropdownSelectLabelItem (Figma: `atom/DropdownSelect/Label/Item`, `1444:21704`)

## Uso

Item de rodapé ("+ Nova Etiqueta") usado nas listas de dropdown de etiqueta — `molecule/Label` e `molecule/DropdownSelectLabel` reusam o mesmo item pra oferecer a ação de criar uma etiqueta nova ao final da lista.

<Canvas of={DropdownSelectLabelItemStories.Idle} />
<Controls />

<Canvas of={DropdownSelectLabelItemStories.AllStates} />

## Estados

`Idle` · `Hover` (via `:hover` nativo) · `Clicked` (prop `active`). Sem disabled/loading/error.
