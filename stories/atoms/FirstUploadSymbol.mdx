import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as FirstUploadSymbolStories from './FirstUploadSymbol.stories';

<Meta of={FirstUploadSymbolStories} />

# FirstUploadSymbol (`atom/firstUploadSymbol`)

## Uso

Símbolo de upload pro **kickoff inicial** do usuário na plataforma — estado vazio/onboarding ("nenhum arquivo ainda"). 🧩 Inferido pela descrição do Figma; sem tela específica confirmando o contexto exato de uso.

<Canvas of={FirstUploadSymbolStories.Default} />
<Controls />

## Anatomia

267×267px: círculo de fundo sólido (`#D9D9D9`) + glifo `CloudDownload` (`#6B6B68`, 85×85) sobreposto, centralizado no terço inferior. Sem props — elemento puramente decorativo, sem variantes.

## Estados

Só `Default` — elemento estático, sem hover/active/disabled (é decorativo, não um controle).
