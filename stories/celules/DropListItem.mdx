import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as DropListItemStories from './DropListItem.stories';

<Meta of={DropListItemStories} />

# DropListItem (Figma: `celule/dropListItem`, `1440:23803`)

## Uso

Item de lista suspensa (ícone + rótulo) usado dentro do
`organism/dropList`. Peça de composição — use dentro de `DropdownMenu`,
nunca isolado numa tela. O item ocupa a largura total do menu que o
contém.

<Canvas of={DropListItemStories.Idle} />
<Controls />

<Canvas of={DropListItemStories.Active} />
<Canvas of={DropListItemStories.Hover} />

## Estados

- `idle`
- `hover` — fundo cinza claro
- `clicked` — fundo escuro semitransparente

## Terminologia

Texto de exemplo: "Nova pasta".
