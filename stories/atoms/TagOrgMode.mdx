import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as TagOrgModeStories from './TagOrgMode.stories';

<Meta of={TagOrgModeStories} />

# TagOrgMode (Figma: `atom/TagOrgMode`, `1421:18769`)

## Uso

Tag que identifica o **modo de organização** ativo (`Data`/`Livre`/`Projeto`/`Tipo`) — usada nos fluxos de criação/edição de template de organização, pra deixar claro qual critério está sendo aplicado.


**Status: ✅ aligned (Figma-confirmado, US-018)** — protocolo completo da
Regra 11 aplicado: `get_design_context` real no node, screenshot Figma
comparado elemento-a-elemento contra o Storybook renderizado via Playwright.

**Reverificado em 2026-08-12 (US-026, 3ª passada de auditoria de
ponto-fixo)** — `get_design_context` refeito do zero, nenhuma divergência
nova encontrada.

<Canvas of={TagOrgModeStories.Free} />
<Controls />

## Figma (Figma-confirmado)

Descrição verbatim: *"Badge presente no sandbox de organização, tem o
rótulo do modo de template selecionado"*. Usado em
`organism/OrganizeFreeModeCanvas` (sandbox de organização por
data/projeto/tipo/livre).

<Canvas of={TagOrgModeStories.AllModes} />

## Eixo `mode` (4 variantes Figma-confirmadas)

| Valor da prop | Rótulo exibido | Node Figma |
| --- | --- | --- |
| `free` (default) | Modo Livre | `1421:18770` |
| `date` | Data | `1421:18772` |
| `project` | Projeto | `1421:18774` |
| `type` | Tipo | `1421:18776` |

## Cor (Regra 3)

Fundo `neutral-surface-medium` (`#52525b`) bate exatamente com `zinc-600` do
Tailwind — sem aproximação (ver tabela "Paleta neutra" em
`docs/conflicts.md`, um dos 4 papéis neutros com correspondência exata).
Texto `brand-primary-light` (`#c8dce3`) reusa o token já existente
`--brand-teal-light` (mesmo valor hex, achado em US-013) — sem literal novo.

## Tipografia

Figtree Medium `25px` (`Type/H3`) — acima do piso de 16px (Regra 4), sem
exceção necessária. Implementado como `text-[1.5625rem]` (`rem`, mesma
escala Major Third 1.25 a partir de 16px).

## Dimensões (Figma-confirmado)

Altura `36px` (`h-9`), padding `12px`/`6px` (`px-3 py-1.5`), raio `8px`
(`rounded-md`, mapeado ao token `--radius-md` já existente no tema).

## Checklist elemento-a-elemento (Regra 11)

| Elemento Figma | Implementado | Fonte |
| --- | --- | --- |
| Pílula com fundo `zinc-600` | ✅ `bg-zinc-600` | ✅ Figma-confirmado |
| Texto do modo, 25px Figtree Medium | ✅ `text-[1.5625rem] font-medium` | ✅ Figma-confirmado |
| Cor de texto `#c8dce3` | ✅ `text-brand-teal-light` | ✅ Figma-confirmado |
| 4 rótulos de modo | ✅ `MODE_LABEL` | ✅ Figma-confirmado |
| Padding `12px`/`6px`, raio `8px`, altura `36px` | ✅ `px-3 py-1.5 rounded-md h-9` | ✅ Figma-confirmado |

## Estados (Regra 8)

Elemento de exibição estática — o Figma não expõe eixo `state`/interação
para este badge (é um rótulo do sandbox, não um controle clicável).

| Estado | Aplicável? | Nota |
| --- | --- | --- |
| Default | ✅ | Único tratamento visual |
| Hover/Active/Disabled/Loading/Error | ❌ Não aplicável | Sem alvo de ponteiro, não é um controle |

## Material Liquid Glass

Não aplicável — preenchimento de cor sólida, sem uso do material "Liquid
Glass" (Regra 10).

## Fidelidade code-level

Componente 100% reproduzível em código — sem vetores customizados, sem
asset exportado.
