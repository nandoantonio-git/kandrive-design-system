import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as PagesLeadStories from './PagesLead.stories';

<Meta of={PagesLeadStories} />

# PagesLead (Figma: `celule/Pages/Lead`, `1439:17048`)

Cabeçalho de página (título H1 + legenda) — reutilizável em toda página de
nível superior do produto (Armazenamento, Configurações de Plano etc.).

**Status: ✅ aligned (Figma-confirmado, US-021; re-verificado 2026-08-12,
US-026 3ª passada de ponto-fixo)** — protocolo completo da Regra 11
aplicado antes de marcar como verificado. 3ª passada: `get_design_context`
fresco no nó `1439:17048` + screenshot real (stories `Default`/`OtherPage`)
confirmam H1 40px bold + legenda 14px zinc-500 — sem divergência nova.

**Camada `celule`** (AGENTS.md — Estrutura de arquivos): peça própria.

<Canvas of={PagesLeadStories.Default} />
<Controls />

## Figma (Figma-confirmado)

`get_design_context` no nó `1439:17048` retorna descrição verbatim:
*"Titulo e subtitulo das paginas, foco em melhorar SEO."* Instância
amostrada no nó: título "Armazenamento", legenda "Gerencie seu
armazenamento".

<Canvas of={PagesLeadStories.OtherPage} />

Elemento a elemento contra o screenshot do Figma (Regra 11.3):
- H1 "Armazenamento", `Figtree Bold`, `40px`, cor `text-black` (preto puro
  — literal, diferente de `neutral-text-primary` `#09090b` usado em outros
  componentes; preservado literal por Regra 9, não normalizado por
  suposição) — ✅ presente.
- Legenda "Gerencie seu armazenamento", `14px`, cor `#71717a` (bate exato
  com `zinc-500` do Tailwind) — ✅ presente.

## Material Liquid Glass

Não aplicável — texto puro, sem material "Liquid Glass" (Regra 10).

## Estados (Regra 8)

Não aplicável — componente estático (título/legenda de página), sem
interação.

## Fluid interface (Regra 8)

Não aplicável.

## Terminologia

"Armazenamento"/"Gerencie seu armazenamento" são Figma-confirmados
verbatim (instância amostrada). `title`/`caption` são props abertas — cada
página usa seu próprio texto real (ex.: "Configurações de Plano" na story
`OtherPage`, 🧩 inferido como exemplo de reuso, já que o nó Figma só
amostra a página de Armazenamento).

## Fidelidade code-level

`title` sem valor default (obrigatório) — evita que uma página esqueça de
definir seu próprio H1; `caption` mantém o default Figma-confirmado da
amostra ("Gerencie seu armazenamento") por conveniência, mas pode ser
sobrescrito por página.
