import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as DropListItemStories from './DropListItem.stories';

<Meta of={DropListItemStories} />

# DropListItem (Figma: `celule/dropListItem`, `1440:23803`)

## Uso

Item de lista suspensa (ícone + rótulo) usado dentro do `organism/dropList`.
Peça de composição — use dentro de `DropdownMenu`, nunca isolado numa tela.

**Status: ✅ aligned com correção de cobertura (Figma-confirmado, US-021;
re-verificado 2026-08-13, US-026 12ª passada ativa)** — `get_design_context`
fresco no nó `1440:23803` confirma ícone/texto/estados
`idle`/`hover`/`clicked`. Divergência corrigida nesta passada: `hover`
existia só como pseudoestado CSS e não como story estática auditável; agora
`state="hover"` permite screenshot Playwright direto do estado Figma.

**Camada `celule`** (AGENTS.md — Estrutura de arquivos): peça própria.

<Canvas of={DropListItemStories.Idle} />
<Controls />

## Figma (Figma-confirmado)

`get_design_context` no nó `1440:23803` retorna descrição verbatim: *"item
do organism dropList."* 3 estados confirmados (`state`): `idle`, `hover`
(fundo `var(--neutral-border-subtle,#ececf0)`), `clicked` (fundo
`var(--effect-overlay-md,rgba(0,0,0,0.14))`). Texto verbatim confirmado:
"Nova pasta".

<Canvas of={DropListItemStories.Active} />
<Canvas of={DropListItemStories.Hover} />

Elemento a elemento contra o screenshot do Figma (Regra 11.3):
- Ícone de pasta com "+" à esquerda (`Container`, `fill="#6B6B68"`
  `fill-opacity="0.45"`) — ✅ presente, substituído por `lucide-react`
  `FolderPlus` (mapeamento por nome/significado, Regra 9 — não vetor
  pixel-a-pixel, mesmo critério de `stories/atoms/Icon.mdx`).
- Texto "Nova pasta", `Figtree Medium 16px`, cor `#3f3f46` (bate exato com
  `zinc-700` do Tailwind) — ✅ presente.
- Fundo cinza claro no hover, fundo escuro semitransparente no clicado —
  ✅ presente (`state="hover"`/`state="clicked"` para documentação e
  `:hover` do browser para uso real).

## Material Liquid Glass

Não aplicável — item de lista com fundo sólido/overlay, não o material
"Liquid Glass" (Regra 10).

## Estados (Regra 8)

| Estado | Implementado | Fonte |
| --- | --- | --- |
| Idle | ✅ default | ✅ Figma-confirmado |
| Hover | ✅ `state="hover"` + `:hover` do browser (`bg-[#ececf0]`) | ✅ Figma-confirmado |
| Clicked | ✅ `state="clicked"`/prop `active` (`bg-black/14`) | ✅ Figma-confirmado |
| Disabled/Loading | ❌ Não aplicável | Sem eixo próprio confirmado no nó |

## Fluid interface (Regra 8)

`transition-colors` no fundo (mesmo padrão de
`atom/DropdownSelect/GroupBy/Item`). Reduced-motion não documentado no
Figma.

## Terminologia

Texto "Nova pasta" é Figma-confirmado verbatim. Nenhum termo da lista
proibida (Regra 5) se aplica.

## Fidelidade code-level

Largura fixa do nó (`191px`) trocada por `w-full` — 🧩 inferido, item de
lista deve esticar para a largura do `organism/dropList` que o contém
(sem organism próprio implementado ainda para confirmar a largura real de
contêiner).
