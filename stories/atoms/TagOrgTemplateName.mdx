import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as TagOrgTemplateNameStories from './TagOrgTemplateName.stories';

<Meta of={TagOrgTemplateNameStories} />

# TagOrgTemplateName (Figma: `atom/TagOrgTemplateName`, `1421:18778`)

## Uso

Tag com o nome do template de organização, em 2 estados: preenchida (nome real) ou placeholder (template ainda sem nome) — usada junto de `TagOrgMode` nos fluxos de organização.

<Canvas of={TagOrgTemplateNameStories.Placeholder} />
<Controls />

## Anatomia

É um `<input type="text">` real, não um texto estático — o usuário digita o nome do template diretamente no chip. Placeholder padrão: "Adicionar Nome".

## Estados

`placeholder` (vazio) · `preenchido` (com valor) · `focus`
