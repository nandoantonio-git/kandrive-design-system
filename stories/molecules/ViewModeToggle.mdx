import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as ViewModeToggleStories from './ViewModeToggle.stories';

<Meta of={ViewModeToggleStories} />

# ViewModeToggle (Figma: `molecule/view-mode-toggle`, `1421:19069`)

## Uso

Pílula de alternância do modo de visualização do grid de arquivos
(Grid/List/Columns). Use no cabeçalho de qualquer tela com listagem de
arquivos que suporte mais de um layout — se a tela só tem um layout
possível, não adicione o toggle.

<Canvas of={ViewModeToggleStories.Grid} />
<Controls />

## Anatomia

Rótulo de seção "VISUALIZAR" + 3 botões (Grid/List/Columns), cada um com
ícone + label, sobre fundo Liquid Glass. O botão do modo ativo tem
destaque de fundo e cor de texto de marca.

<Canvas of={ViewModeToggleStories.List} />
<Canvas of={ViewModeToggleStories.Columns} />

## Estados

| Estado | Nota |
| --- | --- |
| Selecionado | Fundo escuro + texto na cor de marca clara |
| Não selecionado | Texto neutro |
| Hover | Leve destaque de fundo |
| Disabled | Não implementado |

## Terminologia

Rótulos "Grid"/"List"/"Columns" e "VISUALIZAR" — mistura de inglês e
português preservada do conteúdo original.
