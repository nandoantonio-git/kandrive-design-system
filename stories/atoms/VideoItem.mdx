import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as VideoItemStories from './VideoItem.stories';

<Meta of={VideoItemStories} />

# VideoItem (Figma: `atom /VideoItem`, `1442:7858`)

## Uso

Linha/card compacto para representar um vídeo dentro de uma lista de arquivos — mesma anatomia de `ArchiveItem`/`FolderItem`/`ImageItem`. A variante `favicon-header-thumbnail` omite o controle de seleção de propósito (contexto de thumbnail, não de lista selecionável).

<Canvas of={VideoItemStories.Idle} />
<Controls />

<Canvas of={VideoItemStories.AllStates} />

## Estados

`idle` · `hover` · `pressed` · `disabled` · `selected` · `selected-hover` · `selected-pressed` · `favicon-header-thumbnail`

## Interação

Sem `state` fixo — passe o mouse, clique/pressione Enter pra selecionar de verdade (não é freeze-frame). `state` continua disponível como override pra congelar um estado específico (é o único jeito de chegar em `favicon-header-thumbnail`, que não faz parte do ciclo real de interação).

<Canvas of={VideoItemStories.Interactive} />
