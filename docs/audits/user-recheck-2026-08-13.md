# User recheck — mandatory US-026 checklist

Date: 2026-08-13
Status: **CLEAN (pass20, 2026-08-14)** — the 2 remaining rows (`organism/FAQ/FastLinks`, `molecule/Label`) received fresh `get_design_context` and moved to `verified-clean`. `FastLinks` had a real material finding (root frame padding: Figma confirms zero horizontal padding, an earlier pass had added an unwarranted `px-4` that made PT translations wrap to 2 lines) — fixed and reverified. All 16 rows below are now `verified-clean`. See `docs/audits/us-026-pass20.md`.

Prior status (superseded): NON-CLEAN — pass19 gave all 15 remaining rows fresh get_design_context within the pass and found+fixed a real material finding (`atom/CloseButton` rendered with no visible "×" glyph in any state/size). See `docs/audits/us-026-pass19.md`.

Every Ralph provider must read this checklist before US-026. A row moves from `pending` to `corrected-needs-clean-pass`, then to `verified-clean` only with fresh Figma context, isolated Storybook evidence, consumer evidence when applicable, and an element-by-element checklist.

| Area | Required outcome | Status |
| --- | --- | --- |
| Story backgrounds | Remove black backgrounds invented by decorators; retain dark surfaces only where current Figma confirms them. | **verified-clean (pass20)** — full recapture of all 276 states / 84 components (`shot-pass20-full.tmp.mjs`, 276/276 succeeded) reviewed via contact sheets; no invented dark/black background found in any component. Decorators remain neutral-surface wrappers. |
| `atom/CloseButton` | Repair visual states and hit target separately; reverify every consumer. | **verified-clean (pass20)** — pass19 material finding (fresh `get_design_context` on `1421:19008` revealed the "×" glyph, node `172:3689`, is a separate overlay in Figma, never baked into the exported circle SVGs; the button rendered as a plain solid dot with no glyph in every size/state) fixed by overlaying `ClearButtonGlyph` at Figma-confirmed size (SM 6px/MD 12px) and per-state opacity (idle 100%/hover 32%/pressed 20%). Pass20 reconfirmed visually in `atoms-closebutton--all-states.png` — glyph visible in all 6 states. See `docs/audits/us-026-pass19.md`, `docs/audits/us-026-pass20.md`. |
| Sidebar | Add missing top margin. Collapse has its own upper row; `Adicionar` is on a separate row below. They must not share a row. | verified-clean (pass19) — fresh Figma read `1421:17946`/`1421:17947`; collapse row and add row remain separated in code (`sidebar-collapse-row`/`sidebar-add-row`) and screenshot; evidence `organisms-sidebar--default.png` sha `5d1dda078830`. |
| `organism/cleanSpaceStorage` | Re-read node `1439:16908`; reconcile changed icons, layout and states. Old evidence is invalid. | verified-clean (pass19) — fresh Figma read `1439:16908`; header close still reconciled to `atom/CloseButton` (now with visible glyph); evidence `organisms-cleanspacestorage--default.png` sha `bcf8c93e84d3`. |
| `celule/cleanSpaceStorage/listSelection` | Re-read node `1436:20496`, download current assets and reverify organism consumers. | verified-clean — fresh Figma read `1436:20496` (reconfirmed pass19), no divergence vs current code. |
| `organism/SearchToolbar` | Remove from current public catalog: inferred, no confirmed node, no code consumers. Preserve historical logs. | verified-clean — source and public stories remain removed; not part of the current catalog. |
| `organism/OrganizePanel/DropZone` | Reconcile current Mode × State × Quantity variants and file-item consumers. | verified-clean (pass19) — fresh Figma read `1421:18781`, no divergence vs current code. |
| `organism/MainCanvas/Organization/FreeMode` | Replace approximate minimap with current Figma-confirmed structure. | verified-clean (pass19) — fresh Figma read `1439:16906`, `Details` fixed at `34.5px` overflow-hidden and Mini-Map positioning still matches; no new divergence. |
| Header — Organizar | Replace Lucide approximation with real current Figma icon; reverify Header. | verified-clean (pass19) — fresh Figma read `1421:19918`, `Organizar` still uses exported `ICONS.Organize`; no new divergence. |
| `organism/FileListContainer` | Glyphs have no visible name in/under icon; glyphs are `aria-hidden`, filename stays only in separate text region. | verified-clean (pass19) — fresh Figma read `1421:19687`/`1421:19200`; `FileList` still hides the icon micro-name in `list-sm`; no new divergence found (raw Figma instance shows inconsistent per-row overrides of the micro-name text, treated as authoring noise per prior pass's documented reasoning, not reopened per audit rules). |
| `organism/FAQ/FastLinks` | Add missing padding using current Figma measurements. | **verified-clean (pass20)** — fresh `get_design_context` on `1454:25006` this pass revealed the opposite of the original framing: the Figma root frame has zero horizontal padding (`py-[24px]` only, no `px`); an earlier pass had added an unwarranted `px-4`, narrowing the text area enough to wrap the (longer) PT translations to 2 lines. Removed `px-4`, width adjusted to `w-[158px]`. Evidence: `organisms-faqfastlinks--default.png`, all 3 links now single-line. See `docs/audits/us-026-pass20.md`. |
| `organism/Dialog/TemplateReviewModal` | Add inferred/documented hover and momentary clicked states for Editar/Renomear, with interruptible feedback and reduced-motion; clicked is not persistent selection. | verified-clean (pass19) — fresh Figma read `1431:20397`; hover/focus and `motion-safe` active feedback still present; no new divergence. |
| `molecule/StorageStatus` | Tags use current semantic Figma colors, not gray inactive styling. | verified-clean — fresh Figma read `1421:18354` (pass18), not reopened. |
| `molecule/Label` | `Etiquetar` remains fully visible and nowrap, neither wrapped nor truncated. | **verified-clean (pass20)** — fresh `get_design_context` on `1421:18687` this pass. Code already carried the pass14 fix (`whitespace-nowrap`); no further change needed. Evidence: `molecules-label--default.png`, `--expanded.png`, `--disabled.png` — "Etiquetar" fully visible, no wrap/truncation in all 3 states. See `docs/audits/us-026-pass20.md`. |
| `ArchiveItem`, `ImageItem`, `FolderItem`, `VideoItem` | Reverify isolated atoms and every real consumer: size, filename, selection, badge and states. Do not invent Folder/Video consumers. | verified-clean (pass19) — all 4 freshly re-read (`1421:18214`/`1440:24306`/`1421:18311`/`1442:7858`) and conferred element-by-element against current code; no new divergence. `VideoItem`'s `favicon-header-thumbnail` still correctly omits the selection control. |

## Audit rules

- This is a starting set, not a ceiling; additional material divergence also makes the pass NON-CLEAN.
- Work dependency-first: shared primitives/icons, atoms/celules, molecules, organisms, then impacted consumers.
- Current user feedback and current Figma supersede prior aligned claims. Explicit human decisions in `docs/conflicts.md` remain authoritative unless this checklist explicitly replaces them.
- Never declare CLEAN while any row is `pending` or `corrected-needs-clean-pass`.
- No commit, push, deploy, PR or Figma write.
